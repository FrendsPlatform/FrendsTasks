name: Common Toolkit Build

on:
  push:
    branches:
      - '**'
    paths:
      - 'Frends.Common.Toolkit/**'
  workflow_dispatch:

jobs:
  build:
    uses: FrendsPlatform/FrendsTasks/.github/workflows/toolkit_build.yml@fspw-732-CommonLib-PoC
    with:
      working-directory: 'Frends.Common.Toolkit'
      project-directory: 'Frends.Common.Toolkit'
