name: Echo Toolkit Release
permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - 'Company.Echo.Toolkit/**'
  workflow_dispatch:

jobs:
  build:
    uses: FrendsPlatform/FrendsTasks/.github/workflows/toolkit_release.yml@main
    with:
      project-directory: 'Company.Echo.Toolkit/Company.Echo.Toolkit'
    secrets: inherit
