name: Echo Toolkit Test
permissions:
  contents: read

on:
  push:
    branches:
      - '**'
    paths:
      - 'Company.Echo.Toolkit/**'
  workflow_dispatch:

jobs:
  build:
    uses: FrendsPlatform/FrendsTasks/.github/workflows/toolkit_test.yml@main
    with:
      working-directory: 'Company.Echo.Toolkit'
      project-directory: 'Company.Echo.Toolkit'
    secrets: inherit
